from setuptools import setup, find_packages

setup(
    name="jupyter_mcp",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.10",
    description="Jupyter Lab ⟷ OpenClaw MCP bridge",
)
